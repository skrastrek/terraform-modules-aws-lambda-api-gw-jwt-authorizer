{
  "name": "lambda-api-gw-jwt-authorizer",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "aws-jwt-verify": "^5.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.153",
    "@types/node": "^24.6.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.899.0",
    "esbuild": "^0.25.10"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node22 --outdir=dist"
  }
}
